from django.db import models

class Upload_image(models.Model):
    photo = models.ImageField(upload_to="/aiffin",null=True, blank=True)
    link=models.TextField(blank=True,default='')
    name = models.CharField(blank=True,max_length=100,default='',editable=True)

